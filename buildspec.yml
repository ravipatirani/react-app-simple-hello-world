version: 0.2

phases:
  pre_build:
    commands:
      - npm install
  build:
    commands:
      - npm build
      
artifacts:
  files:
    - '**/*'

reports:
  jest_reports:
    files:
      - jest tests
    file-format: JUNITXML
    base-directory: "."
