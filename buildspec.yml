version: 0.2

phases:
  pre_build:
    commands:
      - npm install
  build:
    commands:
      - npm build
      - npm test

artifacts:
  files:
    - '**/*'

reports:
  jest_reports:
    files:
      - jest tests
    file-format: JUNITXML
    base-directory: ./artifacts
